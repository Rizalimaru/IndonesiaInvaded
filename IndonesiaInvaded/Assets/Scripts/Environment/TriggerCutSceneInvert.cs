using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TriggerCutSceneInvert : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        EnvironmentCutSceneInvert.instance.CameraCutScene();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
